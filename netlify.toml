[build]
  command = "npx expo export --platform web --output-dir dist && cp -r overview dist/"
  publish = "dist"

[build.environment]
  EXPO_PUBLIC_API_BASE_URL = "https://portionist.up.railway.app"
